import { ScrollView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function TabOrder() {
  return (
    <ScrollView>
        <Text>TabOrderrrr</Text>
    </ScrollView>
  )
}

const styles = StyleSheet.create({})