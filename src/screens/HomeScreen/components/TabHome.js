import { ScrollView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function TabHome() {
  return (
    <ScrollView>
        <Text>Tab Homeeeee</Text>
    </ScrollView>
  )
}

const styles = StyleSheet.create({})